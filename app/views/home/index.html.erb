<div class="max-w-[85rem] px-4 py-10 sm:px-6 lg:px-8 lg:py-14 mx-auto" data-controller="calagem">
  <div class="grid lg:grid-cols-3 gap-6">

    <div class="flex flex-col bg-white border shadow-sm rounded-xl p-4 md:p-5 dark:bg-slate-900 dark:border-gray-700 dark:shadow-slate-700/[.7]">
      <h2 class="text-lg font-bold text-gray-800 dark:text-white mb-4 border-b pb-2">
        Parâmetros da análise
      </h2>

      <div class="grid grid-cols-2 gap-4">
        <%
          params_list = [
            { id: "CTC", label: "CTC" },
            { id: "PRNT", label: "PRNT (%)" },
            { id: "Ve", label: "V<sub>e</sub> (%)" },
            { id: "Va", label: "V<sub>a</sub> (%)" },
            { id: "Al", label: "Al (cmol<sub>c</sub>/dm<sup>3</sup>)" },
            { id: "Ca", label: "Ca (cmol<sub>c</sub>/dm<sup>3</sup>)" },
            { id: "Mg", label: "Mg (cmol<sub>c</sub>/dm<sup>3</sup>)" },
            { id: "Y", label: "Y*" },
            { id: "X", label: "X (cmol<sub>c</sub>/dm<sup>3</sup>) *" },
            { id: "mt", label: "m<sub>t</sub> (%) *" }
          ]
        %>

        <% params_list.each do |item| %>
          <div>
            <label for="<%= item[:id] %>" class="block text-sm font-medium mb-2 dark:text-white">
              <%= item[:label].html_safe %>
            </label>
            <input type="number"
                   id="<%= item[:id] %>"
                   data-calagem-target="input"
                   data-param-name="<%= item[:id] %>"
                   step="0.01"
                   class="py-2 px-3 block w-full border-gray-200 rounded-lg text-sm focus:border-blue-500 focus:ring-blue-500 disabled:opacity-50 disabled:pointer-events-none dark:bg-slate-900 dark:border-gray-700 dark:text-gray-400 dark:focus:ring-gray-600"
                   placeholder="0.00">
          </div>
        <% end %>
      </div>

      <div class="mt-4 pt-2 border-t border-gray-100 dark:border-gray-800">
        <p class="text-[10px] text-gray-500 dark:text-gray-400 leading-tight">
        * <strong>X, Y, m<sub>t</sub>:</strong> Fatores de correção e tolerância específicos para a cultura e região. Consulte o <strong>Quadro 8.1</strong> do manual <em>5<sup>a</sup> Aproximação</em> para obter os valores para o cálculo específico.
        </p>
      </div>

      <div class="grid grid-cols-2 gap-3 mt-6">
        <button type="button"
                data-action="click->calagem#calculate"
                class="py-3 px-4 inline-flex justify-center items-center gap-x-2 text-sm font-semibold rounded-lg border border-transparent bg-blue-600 text-white hover:bg-blue-700 disabled:opacity-50 disabled:pointer-events-none">
          Calcular
        </button>

        <button type="button"
                data-action="click->calagem#reset"
                class="py-3 px-4 inline-flex justify-center items-center gap-x-2 text-sm font-medium rounded-lg border border-gray-200 bg-white text-gray-800 shadow-sm hover:bg-gray-50 disabled:opacity-50 disabled:pointer-events-none dark:bg-slate-900 dark:border-gray-700 dark:text-white dark:hover:bg-gray-800">
          Limpar
        </button>
      </div>
    </div>

    <div class="flex flex-col bg-white border shadow-sm rounded-xl p-4 md:p-5 dark:bg-slate-900 dark:border-gray-700 dark:shadow-slate-700/[.7]">
      <h2 class="text-lg font-bold text-gray-800 dark:text-white mb-4 border-b pb-2 text-center">
        Cálculo de calagem
      </h2>

      <div class="mb-6 p-4 bg-gray-50 rounded-lg border border-gray-200 dark:bg-slate-800 dark:border-gray-700">
        <h3 class="text-sm font-semibold uppercase text-gray-500 dark:text-gray-400 mb-2">
          Saturação de Bases
        </h3>
        <div class="flex items-baseline gap-x-2">
          <span class="text-4xl font-bold text-blue-600 dark:text-blue-500" data-calagem-target="saturacaoResult">0.00</span>
          <span class="text-gray-500 text-sm font-medium dark:text-gray-400">t ha<sup>-1</sup></span>
        </div>
      </div>

      <div class="p-4 bg-gray-50 rounded-lg border border-gray-200 dark:bg-slate-800 dark:border-gray-700">
        <h3 class="text-sm font-semibold uppercase text-gray-500 dark:text-gray-400 mb-2">
          Neutralização Al<sup>+3</sup>
        </h3>
        <div class="flex items-baseline gap-x-2">
          <span class="text-4xl font-bold text-blue-600 dark:text-blue-500" data-calagem-target="neutralizacaoResult">0.00</span>
          <span class="text-gray-500 text-sm font-medium dark:text-gray-400">t ha<sup>-1</sup></span>
        </div>
      </div>
    </div>

    <div class="flex flex-col bg-white border shadow-sm rounded-xl p-4 md:p-5 dark:bg-slate-900 dark:border-gray-700 dark:shadow-slate-700/[.7]">
      <h2 class="text-lg font-bold text-gray-800 dark:text-white mb-4 border-b pb-2">
        Análise de Custos
      </h2>
      <div class="flex flex-auto flex-col justify-center items-center p-4 md:p-5">
        <svg class="size-12 text-gray-300 dark:text-gray-600" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"/>
          <line x1="12" y1="8" x2="12" y2="12"/>
          <line x1="12" y1="16" x2="12.01" y2="16"/>
        </svg>
        <p class="mt-3 text-sm text-gray-500 dark:text-gray-400 text-center">
          Os módulos de custo e frete serão exibidos aqui após a definição dos insumos.
        </p>
      </div>
    </div>

  </div>
</div>
